// Copie este arquivo para "config.js" (na mesma pasta) e preencha sua chave.
// config.js é ignorado pelo git — nunca commite sua chave real.
window.APP_CONFIG = {
  MISTRAL_API_KEY: 'SUA_CHAVE_AQUI',
};
